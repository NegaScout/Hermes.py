CREATE TABLE IF NOT EXISTS WIREGUARD_USERS(
    USER_ID INTEGER PRIMARY KEY,
    USER_NAME VARCHAR(64),
    PUB_KEY VARCHAR(64),
    USER_WG_IP VARCHAR(15),
    USER_WG_HOST_NUMBER INTEGER,
    STAMP_DATE_REGISTERED INTEGER
);